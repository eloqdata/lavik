{
  "commit": "b3c55caa6dc4a79276044f1a3cec1636b881f7f8",
  "source_diff": "diff --git a/include/keylane/storage/detail/hash_codec.h b/include/keylane/storage/detail/hash_codec.h\nindex c3fb8b3..7faf3c3 100644\n--- a/include/keylane/storage/detail/hash_codec.h\n+++ b/include/keylane/storage/detail/hash_codec.h\n@@ -1,6 +1,7 @@\n #pragma once\n \n #include <cstddef>\n+#include <span>\n #include <string>\n #include <string_view>\n #include <vector>\n@@ -60,6 +61,12 @@ class HashValueReader {\n absl::StatusOr<HashValue> DecodeHashValue(std::string_view payload);\n absl::StatusOr<std::string> EncodeHashValue(const HashValue& value);\n \n+// Encodes the same format and preserves the supplied order, without an owned\n+// HashValue intermediate. Views need only remain valid for this synchronous\n+// call; the returned string owns all bytes. No sorting or deduplication occurs.\n+absl::StatusOr<std::string> EncodeHashEntryViews(\n+    std::span<const HashEntryView> entries);\n+\n // Checked size accumulation without allocating the supplied field/value.\n // Used before serialization; failure leaves the caller's running size intact.\n // max_bytes is the destination's capacity bound: a physical group envelope or\ndiff --git a/src/storage/engine/hash_codec.cpp b/src/storage/engine/hash_codec.cpp\nindex f5971d6..cac1f84 100644\n--- a/src/storage/engine/hash_codec.cpp\n+++ b/src/storage/engine/hash_codec.cpp\n@@ -139,14 +139,17 @@ absl::StatusOr<HashValue> DecodeHashValue(std::string_view payload) {\n   return value;\n }\n \n-absl::StatusOr<std::string> EncodeHashValue(const HashValue& value) {\n-  if (value.entries_.empty() ||\n-      value.entries_.size() > std::numeric_limits<std::uint32_t>::max()) {\n+namespace {\n+\n+template <typename Entries>\n+absl::StatusOr<std::string> EncodeEntries(const Entries& entries) {\n+  if (entries.empty() ||\n+      entries.size() > std::numeric_limits<std::uint32_t>::max()) {\n     return absl::OutOfRangeError(\"invalid Hash element count\");\n   }\n   std::string output;\n   std::size_t bytes = kHashValueHeaderBytes;\n-  for (const HashEntry& entry : value.entries_) {\n+  for (const auto& entry : entries) {\n     auto next = AppendHashEntrySize(bytes, entry.field_.size(),\n                                     entry.value_.size(), output.max_size());\n     if (!next.ok()) return next.status();\n@@ -157,10 +160,10 @@ absl::StatusOr<std::string> EncodeHashValue(const HashValue& value) {\n   AppendU64(&output, kHashValueMagic);\n   AppendU32(&output, kStorageFormatVersion);\n   AppendU32(&output, kHashValueHeaderBytes);\n-  AppendU32(&output, static_cast<std::uint32_t>(value.entries_.size()));\n+  AppendU32(&output, static_cast<std::uint32_t>(entries.size()));\n   AppendU32(&output, 0);\n   AppendU64(&output, bytes);\n-  for (const HashEntry& entry : value.entries_) {\n+  for (const auto& entry : entries) {\n     AppendU32(&output, static_cast<std::uint32_t>(entry.field_.size()));\n     AppendU32(&output, static_cast<std::uint32_t>(entry.value_.size()));\n     output.append(entry.field_);\n@@ -169,4 +172,15 @@ absl::StatusOr<std::string> EncodeHashValue(const HashValue& value) {\n   return output;\n }\n \n+}  // namespace\n+\n+absl::StatusOr<std::string> EncodeHashValue(const HashValue& value) {\n+  return EncodeEntries(value.entries_);\n+}\n+\n+absl::StatusOr<std::string> EncodeHashEntryViews(\n+    std::span<const HashEntryView> entries) {\n+  return EncodeEntries(entries);\n+}\n+\n }  // namespace keylane::storage\ndiff --git a/src/storage/engine/hash_tree.cpp b/src/storage/engine/hash_tree.cpp\nindex 8409d5b..cfa4c44 100644\n--- a/src/storage/engine/hash_tree.cpp\n+++ b/src/storage/engine/hash_tree.cpp\n@@ -4,6 +4,7 @@\n #include <random>\n #include <set>\n \n+#include \"absl/container/inlined_vector.h\"\n #include \"impl.h\"\n #include \"keylane/glob.h\"\n #include \"keylane/memory.h\"\n@@ -64,6 +65,60 @@ bool NeedsGroupedHash(const HashValue& value) {\n   return false;\n }\n \n+absl::StatusOr<std::size_t> PrepareReplacementHash(\n+    const HashOperation& operation, HashValue& value,\n+    std::optional<std::string>& compact_payload) {\n+  // Sort request positions, not owned values: duplicate fields use the last\n+  // argument in O(n log n), without loading the old Hash. Inline scratch avoids\n+  // an allocation for small replacements; larger requests use the same sort.\n+  // Only owned bytes leave this synchronous helper. Its scratch and borrowed\n+  // views are dead before the caller awaits publication or another lock.\n+  absl::InlinedVector<std::size_t, 16> order(operation.fields_.size());\n+  for (std::size_t i = 0; i < order.size(); ++i) order[i] = i;\n+  std::sort(order.begin(), order.end(), [&](auto a, auto b) {\n+    return operation.fields_[a] == operation.fields_[b]\n+               ? a < b\n+               : operation.fields_[a] < operation.fields_[b];\n+  });\n+  absl::InlinedVector<HashEntryView, 16> entries;\n+  entries.reserve(order.size());\n+  std::uint64_t bytes = kHashValueHeaderBytes;\n+  for (std::size_t i = 0; i < order.size(); ++i) {\n+    const auto position = order[i];\n+    const auto field = operation.fields_[position];\n+    if (i + 1 < order.size() && field == operation.fields_[order[i + 1]])\n+      continue;\n+    const auto field_value = operation.values_[position];\n+    entries.push_back({field, field_value});\n+    // This is the same promotion decision as NeedsGroupedHash, applied after\n+    // duplicate elimination. Individual sizes were checked before admission;\n+    // stop accumulating at the threshold so large collections cannot overflow.\n+    if (bytes < kGroupedHashPromotionBytes)\n+      bytes += 8 + field.size() + field_value.size();\n+  }\n+  if (bytes < kGroupedHashPromotionBytes) {\n+    auto encoded = EncodeHashEntryViews(entries);\n+    if (!encoded.ok()) return encoded.status();\n+    compact_payload.emplace(std::move(*encoded));\n+    return entries.size();\n+  }\n+\n+  // Group planning still consumes owned entries. Compact replacements instead\n+  // copy request bytes only into their final payload, avoiding per-field heap\n+  // strings and unused process-local digests; the durable encoding is identical.\n+  value.entries_.reserve(entries.size());\n+  for (const auto& view : entries) {\n+    // Construct in the reserved slot to avoid moving temporary SSO strings.\n+    // The caller owns this unpublished value and discards it on allocation\n+    // failure, so a partially initialized entry cannot reach the live index.\n+    auto& entry = value.entries_.emplace_back();\n+    entry.digest_ = ComputeDigest(view.field_);\n+    entry.field_.assign(view.field_);\n+    entry.value_.assign(view.value_);\n+  }\n+  return entries.size();\n+}\n+\n absl::StatusOr<HashResult> ReadCompactHashResult(\n     std::string_view payload, HashOperationKind kind, std::uint64_t count) {\n   auto reader = HashValueReader::Open(payload);\n@@ -457,6 +512,7 @@ Task<absl::StatusOr<HashResult>> StorageEngine::Impl::ExecuteHashLikeLocked(\n       grouped_scratch.emplace(std::move(*admitted));\n     }\n     HashValue compact;\n+    std::optional<std::string> prepared_compact_payload;\n     if (grouped != nullptr) {\n       if (IsPointOperation(operation.kind_)) {\n         // Loading a requested leaf once is enough even when HMGET/HSET mentions\n@@ -636,26 +692,10 @@ Task<absl::StatusOr<HashResult>> StorageEngine::Impl::ExecuteHashLikeLocked(\n \n     switch (operation.kind_) {\n       case HashOperationKind::kReplaceOnly: {\n-        // Sort request positions, not owned values: duplicate fields use the\n-        // last argument, in O(n log n) without reading any old Hash payload.\n-        std::vector<std::size_t> order(operation.fields_.size());\n-        for (std::size_t i = 0; i < order.size(); ++i) order[i] = i;\n-        std::sort(order.begin(), order.end(), [&](auto a, auto b) {\n-          return operation.fields_[a] == operation.fields_[b]\n-                     ? a < b\n-                     : operation.fields_[a] < operation.fields_[b];\n-        });\n-        compact.entries_.reserve(order.size());\n-        for (std::size_t i = 0; i < order.size(); ++i) {\n-          const auto position = order[i];\n-          const auto field = operation.fields_[position];\n-          if (i + 1 < order.size() && field == operation.fields_[order[i + 1]])\n-            continue;\n-          compact.entries_.push_back(\n-              HashEntry{.digest_ = ComputeDigest(field),\n-                        .field_ = std::string(field),\n-                        .value_ = std::string(operation.values_[position])});\n-        }\n+        auto prepared = PrepareReplacementHash(operation, compact,\n+                                               prepared_compact_payload);\n+        if (!prepared.ok()) co_return prepared.status();\n+        result.length_ = *prepared;\n         // Identical replacements still write and invalidate WATCH. Comparing\n         // old values would defeat the explicit read-free replacement contract.\n         result.changed_ = true;\n@@ -875,10 +915,12 @@ Task<absl::StatusOr<HashResult>> StorageEngine::Impl::ExecuteHashLikeLocked(\n         co_return result;\n     }\n \n-    result.length_ = grouped == nullptr\n-                         ? compact.entries_.size()\n-                         : location.logical_size_ - selected_field_count +\n-                               compact.entries_.size();\n+    if (!replace) {\n+      result.length_ = grouped == nullptr\n+                           ? compact.entries_.size()\n+                           : location.logical_size_ - selected_field_count +\n+                                 compact.entries_.size();\n+    }\n     result.key_exists_ = result.length_ != 0;\n     std::optional<HashGroupMutationPlan> grouped_plan;\n     if (unlocked_grouped_write) {\n@@ -900,17 +942,17 @@ Task<absl::StatusOr<HashResult>> StorageEngine::Impl::ExecuteHashLikeLocked(\n               : (tx != nullptr ? &tx->mutation_precondition_ : nullptr));\n       if (!valid.ok()) co_return valid;\n     }\n-    std::optional<std::string> prepared_compact_payload;\n     bool compact_write_promotes = false;\n     if (unlocked_compact_write || unlocked_create) {\n       if (result.changed_ && result.key_exists_) {\n-        compact_write_promotes = NeedsGroupedHash(compact);\n+        compact_write_promotes =\n+            !prepared_compact_payload && NeedsGroupedHash(compact);\n         if (unlocked_create && compact_write_promotes) {\n           auto prepared = PrepareGroupedHashMutation(\n               nullptr, std::move(compact), {}, result.length_, 1);\n           if (!prepared.ok()) co_return prepared.status();\n           grouped_plan.emplace(std::move(*prepared));\n-        } else if (!compact_write_promotes) {\n+        } else if (!compact_write_promotes && !prepared_compact_payload) {\n           auto encoded = EncodeHashValue(compact);\n           if (!encoded.ok()) co_return encoded.status();\n           prepared_compact_payload.emplace(std::move(*encoded));\n@@ -1007,7 +1049,7 @@ Task<absl::StatusOr<HashResult>> StorageEngine::Impl::ExecuteHashLikeLocked(\n     absl::Status written = co_await AppendLocked(\n         store, partition, db_id, key, digest, payload, kind, published_type,\n         kind == RecordKind::kValue ? expire_at_ms : 0, tx,\n-        kind == RecordKind::kValue ? compact.entries_.size() : 0, nullptr,\n+        kind == RecordKind::kValue ? result.length_ : 0, nullptr,\n         nullptr, replication, nullptr, true, nullptr, mutation_precondition);\n     if (!written.ok()) co_return written;\n     co_return result;\n",
  "variants": {
    "baseline": {
      "path": "/mnt/dev/keylane-perf-hreplace.Y97A2k/keylane-baseline",
      "sha256": "8ded8812356d872eabb05bcd0ff828dcf8185771c50ef1d3b566d5d48403ab1d"
    },
    "candidate": {
      "path": "/mnt/dev/keylane-perf-hreplace.Y97A2k/keylane-views",
      "sha256": "4365296b8206f9f3668b4c1e80fe1755e7b95df0c5a1d3118c1a82bed1e25bc5"
    }
  },
  "rounds": 3,
  "order": [
    "baseline",
    "candidate"
  ],
  "workloads": [
    "C",
    "A"
  ],
  "rates": [
    100000,
    0
  ],
  "counts": {
    "warmup": 1000000,
    "measured": 5000000
  },
  "mode": "hreplace",
  "monitoring": "active\nactive\n",
  "note": "Same 12+4 policy, no reload, no perf or compilation during scored runs"
}
